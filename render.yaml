services:
  # Backend API Service
  - type: web
    name: chatbot-backend
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      cd backend && python app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 10000
    healthCheckPath: /api/health

  # Frontend Streamlit Service
  - type: web
    name: chatbot-frontend
    env: python
    plan: free
    buildCommand: |
      pip install streamlit requests
    startCommand: |
      cd frontend && streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: 8501
      - key: BACKEND_API_URL
        fromService:
          name: chatbot-backend
          type: web
          property: url
    healthCheckPath: / 